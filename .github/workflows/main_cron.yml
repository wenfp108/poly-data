name: ⚔️ PolyData Dual-Engine System

on:
  schedule:
    - cron: '15 * * * *'  # 每小时 15 分自动触发
  workflow_dispatch:      # 允许手动触发

jobs:
  engage_targets:
    runs-on: ubuntu-latest
    
    env:
      MY_PAT: ${{ secrets.MY_PAT }}
      REPO_OWNER: ${{ github.repository_owner }}
      GITHUB_REPOSITORY: ${{ github.repository }}
      PUPPETEER_CACHE_DIR: ${{ github.workspace }}/.cache/puppeteer

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          # cache: 'npm'  <-- ❌ 删除这行，因为没有 lock 文件无法缓存

      # 安装 Chrome 依赖 (保持不变)
      - name: Install System Dependencies for Chrome
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            ca-certificates \
            fonts-liberation \
            libasound2 \
            libatk-bridge2.0-0 \
            libatk1.0-0 \
            libc6 \
            libccairo2 \
            libcups2 \
            libdbus-1-3 \
            libexpat1 \
            libfontconfig1 \
            libgbm1 \
            libgcc1 \
            libglib2.0-0 \
            libgtk-3-0 \
            libnspr4 \
            libnss3 \
            libpango-1.0-0 \
            libpangocairo-1.0-0 \
            libstdc++6 \
            libx11-6 \
            libx11-xcb1 \
            libxcb1 \
            libxcomposite1 \
            libxcursor1 \
            libxdamage1 \
            libxext6 \
            libxfixes3 \
            libxi6 \
            libxrandr2 \
            libxrender1 \
            libxss1 \
            libxtst6 \
            lsb-release \
            wget \
            xdg-utils

      - name: Install Project Dependencies
        run: npm install  # ✅ 修改处：从 npm ci 改为 npm install (自动生成 lock 文件)

      # 🎯 Phase 1: Sniper
      - name: 🎯 Run Sniper Engine
        run: node sniper.js
        timeout-minutes: 10

      # ⏳ 战术暂停
      - name: ⏳ Tactical Pause
        run: |
          echo "Cooling down engines..."
          sleep 10

      # 📡 Phase 2: Radar
      - name: 📡 Run Radar Engine
        run: node radar.js
        timeout-minutes: 10
