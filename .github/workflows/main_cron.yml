name: ⚔️ PolyData Dual-Engine System

on:
  schedule:
    - cron: '15 * * * *'  # 每小时 15 分自动触发
  workflow_dispatch:      # 允许手动触发

jobs:
  engage_targets:
    runs-on: ubuntu-latest
    
    # 设置全局环境变量
    env:
      MY_PAT: ${{ secrets.MY_PAT }}
      REPO_OWNER: ${{ github.repository_owner }}
      GITHUB_REPOSITORY: ${{ github.repository }}
      # 强制 Puppeteer 使用项目内的缓存路径
      PUPPETEER_CACHE_DIR: ${{ github.workspace }}/.cache/puppeteer

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # 🔥【新增】安装 Chrome 运行必须的系统库 (缺少这步必报错)
      - name: Install System Dependencies for Chrome
        run: |
          sudo apt-get update
          sudo apt-get install -y ca-certificates fonts-liberation libasound2 libatk-bridge2.0-0 libatk1.0-0 libc6 libccairo2 libcups2 libdbus-1-3 libexpat1 libfontconfig1 libgbm1 libgcc1 libglib2.0-0 libgtk-3-0 libnspr4 libnss3 libpango-1.0-0 libpangocairo-1.0-0 libstdc++6 libx11-6 libx11-xcb1 libxcb1 libxcomposite1 libxcursor1 libxdamage1 libxext6 libxfixes3 libxi6 libxrandr2 libxrender1 libxss1 libxtst6 lsb-release wget xdg-utils

      - name: Install Project Dependencies
        run: npm ci

      # 🔥 第一阶段：狙击手出动
      - name: 🎯 Phase 1: Sniper Engine
        run: node sniper.js
        timeout-minutes: 10

      # ⏳ 战术暂停
      - name: ⏳ Tactical Pause
        run: |
          echo "Cooling down engines..."
          sleep 10

      # 🔥 第二阶段：雷达扫描
      - name: 📡 Phase 2: Radar Engine
        run: node radar.js
        timeout-minutes: 10
